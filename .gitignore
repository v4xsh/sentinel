# Large CSVs / parquet caches (not committed)
data/raw/transactions.csv
data/raw/identity.csv
data/parquet/**
data/llm_cache/**
data/graph_load/**
# Secrets
.env
.env.local
# Python
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/
.ruff_cache/
# OS + IDE
.DS_Store
.idea/
.vscode/
*.swp
# Temporary
/tmp/
*.tmp
# Backtest outputs are useful reference — keep predictions_*.jsonl (small)
# but exclude any large per-case dumps.
backtest/cases/**
# Claude Code runtime artifacts
.claude/
cases/_summary_20.json
backtest/oot/
# Private working notes (JUDGE_REVIEW iterations, CHECKPOINT drafts,
# phase0..phase3 diagnostic scripts) — never pushed.
private/
